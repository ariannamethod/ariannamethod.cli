# Build artifacts — `make` output, never committed (regenerated by clone+make)
/mhx
*.o
*.a
*.so
*.dylib

# bake/aml
bake/aml/runner/aml
bake/aml/tools/amlc

# bake/notorch
bake/notorch/*.a

# vendor/dario (heart binary)
vendor/dario/dario

# bake/mesh-agent (Go binary)
bake/mesh-agent/mesh-agent

# example trainer binaries
examples/nanollama/nanollama
examples/nanollama/*.bin

# macOS / editors
.DS_Store
*.swp
